{
  "name": "contract",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "upgrade_execute": "truffle migrate upgrade_execute --reset --skip-dry-run --network",
    "upgrade_factory": "truffle migrate upgrade_factory --reset --skip-dry-run --network",
    "deploy_factory": "truffle migrate deploy_factory --reset --skip-dry-run --network",
    "setup": "truffle migrate setup --reset --skip-dry-run --network",
    "proxies": "truffle migrate initialise_proxies --reset --skip-dry-run --network",
    "dry_run": "truffle migrate upgrade_execute --reset --skip-dry-run --network local",
    "clean_migration": "truffle networks --clean"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@truffle/hdwallet-provider": "1.4.3",
    "@uniswap/v3-periphery": "1.1.1",
    "ethers": "5.6.2",
    "fs-extra": "9.0.1",
    "request-promise": "4.2.5",
    "solc": "0.7.5",
    "truffle": "5.1.10",
    "truffle-hdwallet-provider-privkey": "0.3.0",
    "truffle-privatekey-provider": "1.3.0"
  },
  "devDependencies": {
    "prettier": "2.2.1",
    "solc": "0.7.5",
    "prettier-plugin-solidity": "1.0.0-beta.2"
  }
}
