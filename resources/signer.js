/**
 *
 * If you're a signer to registry upgrades, please specify your private key here.
 * Then produce the upgrade signature using manage-upgrade.sh
 *
 * The sign-upgrade.sh tool will use yor private key to sign the specified ugprade
 * configurations selected.
 *
 * Send the signature to the deployer of the upgrade so they can include the signature
 * to the quorum.
 *
 */
module.exports = "";
